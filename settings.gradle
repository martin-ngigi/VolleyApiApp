pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven {
            url  "https://jitpack.io"
        }
        //Add this so as to solve some dependancy errors
        jcenter()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url  "https://jitpack.io"
        }
        //Add this so as to solve some dependancy errors
        jcenter()
    }
}
rootProject.name = "VolleyApiApp"
include ':app'
